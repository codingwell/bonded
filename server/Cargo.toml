[package]
name = "bonded-server"
version = "0.1.0"
edition = "2021"
description = "Bonded server — multi-connection aggregation endpoint"
license = "TBD"

[dependencies]
tokio = { version = "1", features = ["full"] }
tracing = "0.1"
tracing-subscriber = "0.3"

[dev-dependencies]
